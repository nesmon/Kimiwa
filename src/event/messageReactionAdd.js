const kimiwaHelp = require('./../kimiwaHelper');

module.exports = class MessageReactionAdd {
    constructor(client) {
        this.kimiwa = client;
    }

    async run(message, emoji) {
        // if (emoji.name === '⬆') {
        //     console.log('more reputation add');
        // }
    }
};